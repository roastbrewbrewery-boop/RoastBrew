# Dependencies
# This is the most important folder to ignore. It contains all your project's
# dependencies, can be very large, and should be re-installed using `npm install`.
/node_modules
/jspm_packages

# Production build output
# These folders typically contain the final, bundled code for deployment.
# They are generated from your source code, so they don't need to be tracked.
/build
/dist

# Log files
# Log files are created by your application or tools and are not part of the source code.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage reports
# These are generated when you run tests and show how much of your code is tested.
coverage
.nyc_output

# Compiled binary addons
# For native Node.js modules that get compiled on your machine.
build/Release

# Editor directories and files
# Ignores configuration files from common code editors like VS Code, WebStorm, etc.
.idea
.vscode/*
!/.vscode/settings.json
!/.vscode/tasks.json
!/.vscode/launch.json
!/.vscode/extensions.json
*.sublime-workspace

# System files
# Ignores temporary files created by your operating system (macOS and Windows).
.DS_Store
Thumbs.db

# Environment variables
# CRITICAL: This prevents your secret keys, passwords, and other sensitive
# information from being uploaded to any version control system.
.env
.env.*.local